import styled from "styled-components";

export const PropertyImageScrollingWrapper = styled.div`
 
    flex:30;
    border-radius: 10px;
    img{
        width:100%;
        border-radius: inherit;
    }
`;